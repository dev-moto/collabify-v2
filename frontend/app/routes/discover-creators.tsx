import { Link } from "react-router";
import { Badge, Button, Card, SearchBox, StatusPanel } from "~/components/ui";
import { creators } from "~/data/mock";

export function meta() { return [{ title: "Discover creators | Collabify" }]; }
export default function DiscoverCreators() { return <main className="min-h-screen bg-slate-50 px-4 py-8 text-slate-950 dark:bg-slate-950 dark:text-white"><div className="mx-auto max-w-7xl"><h1 className="text-3xl font-black">Discover creators</h1><p className="mt-2 text-slate-600 dark:text-slate-300">Search public-safe creator profiles by city, niche, and availability.</p><div className="mt-6 grid gap-3 md:grid-cols-[1fr_auto_auto]"><SearchBox placeholder="Search creators, niches, or city" /><select aria-label="Filter by city" className="rounded-full border border-slate-200 bg-white px-4 py-3 dark:border-white/10 dark:bg-white/10"><option>All cities</option><option>Cebu</option><option>Quezon City</option></select><Button type="button">Apply filters</Button></div><div className="mt-6 grid gap-5 md:grid-cols-3">{creators.length ? creators.map((c) => <Card key={c.id}><Badge tone="green">Public fields only</Badge><h2 className="mt-4 text-xl font-black">{c.name}</h2><p className="text-sm text-slate-600 dark:text-slate-300">{c.city} area · {c.followers} · {c.engagement}</p><div className="mt-4 flex flex-wrap gap-2">{c.niches.map((n) => <Badge key={n} tone="violet">{n}</Badge>)}</div><Link className="mt-5 inline-flex font-bold text-cyan-700" to={`/creators/${c.id}`}>View showcase</Link></Card>) : <StatusPanel type="empty" title="No creators found" message="Try fewer filters or a nearby city." />}</div></div></main>; }
