import { Calendar, MapPin, MessageSquareText, Star } from "lucide-react";
import { Link, useParams } from "react-router";
import { Badge, Button, Card, StatusPanel, VerifiedMark } from "~/components/ui";
import { creators } from "~/data/mock";

export function meta() { return [{ title: "Creator profile | Collabify" }]; }
export default function CreatorProfile() { const { id } = useParams(); const c = creators.find((item) => item.id === id) ?? creators[0]; if (!c) return <StatusPanel type="error" title="Creator not found" message="This public-safe profile is unavailable." />; return <main className="min-h-screen bg-slate-50 px-4 py-8 text-slate-950 dark:bg-slate-950 dark:text-white"><div className="mx-auto max-w-6xl"><Link to="/discover/creators" className="text-sm font-bold text-cyan-700">← Back to discovery</Link><section className="mt-6 rounded-[2rem] bg-gradient-to-br from-slate-950 to-violet-950 p-8 text-white"><Badge tone="cyan">Public creator showcase</Badge><h1 className="mt-4 text-4xl font-black">{c.name}</h1><p className="mt-2 text-cyan-100">{c.handle} · <MapPin className="inline h-4 w-4" /> {c.city} area only</p><p className="mt-4 max-w-2xl text-slate-200">{c.bio}</p><div className="mt-6 flex flex-wrap gap-2">{c.niches.map((n) => <Badge key={n} tone="violet">{n}</Badge>)}</div></section><div className="mt-6 grid gap-6 lg:grid-cols-3"><Card><Star className="h-6 w-6 text-amber-500" /><p className="mt-3 text-3xl font-black">{c.followers}</p><p className="text-sm text-slate-600">Followers across linked socials</p></Card><Card><p className="text-3xl font-black">{c.engagement}</p><p className="text-sm text-slate-600">Engagement estimate</p></Card><Card><Calendar className="h-6 w-6 text-cyan-500" /><p className="mt-3 font-black">{c.available}</p><p className="text-sm text-slate-600">Availability</p></Card><Card className="lg:col-span-2"><h2 className="text-xl font-black">Portfolio highlights</h2><div className="mt-4 grid gap-3 md:grid-cols-3">{c.portfolio.map((p) => <div key={p} className="rounded-2xl bg-slate-50 p-4 dark:bg-white/10">{p}</div>)}</div></Card><Card><h2 className="text-xl font-black">Collaboration preferences</h2><p className="mt-3 text-sm text-slate-600 dark:text-slate-300">{c.rate}. Exact address and coordinates are never shown publicly.</p><Button className="mt-5" type="button"><MessageSquareText className="h-4 w-4" /> Request intro</Button></Card></div></div></main>; }
