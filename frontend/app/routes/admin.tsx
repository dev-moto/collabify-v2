import { Flag, ScrollText, ShieldCheck, Users } from "lucide-react";
import { AppShell, Badge, Card, ProtectedRoute, Stat } from "~/components/ui";
import { auditLogs, businesses } from "~/data/mock";

export function meta() { return [{ title: "Admin | Collabify" }]; }
export default function Admin() { return <ProtectedRoute><AppShell role="admin" title="Admin review center" description="Admin-ready surfaces for verification review, reports, audit logs, and user moderation. Frontend role checks are UX only."><div className="grid gap-5 md:grid-cols-4"><Stat icon={ShieldCheck} label="Pending verifications" value="4" /><Stat icon={Flag} label="Open reports" value="2" /><Stat icon={Users} label="Moderation queue" value="7" /><Stat icon={ScrollText} label="Audit events" value="24" /></div><div className="mt-6 grid gap-6 lg:grid-cols-2"><Card><h2 className="text-xl font-black">Verification review</h2><div className="mt-4 grid gap-3">{businesses.map((b) => <article key={b.id} className="rounded-2xl bg-slate-50 p-4 dark:bg-white/10"><div className="flex justify-between gap-3"><b>{b.name}</b><Badge tone={b.verified ? "green" : "amber"}>{b.verified ? "Approved" : "Review"}</Badge></div><p className="text-sm text-slate-600 dark:text-slate-300">{b.industry} · {b.city}</p></article>)}</div></Card><Card><h2 className="text-xl font-black">Audit logs</h2><ul className="mt-4 space-y-3">{auditLogs.map((log) => <li key={log} className="rounded-2xl bg-slate-50 p-3 text-sm dark:bg-white/10">{log}</li>)}</ul></Card></div></AppShell></ProtectedRoute>; }
