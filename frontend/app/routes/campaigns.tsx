import { ClipboardCheck } from "lucide-react";
import { AppShell, Badge, Button, Card, Field, ProtectedRoute } from "~/components/ui";
import { campaigns } from "~/data/mock";

export function meta() { return [{ title: "Campaigns and x-deals | Collabify" }]; }
export default function Campaigns() { return <ProtectedRoute><AppShell title="Campaigns and x-deals" description="Manage offers, deliverables, x-deal details, notes, and campaign progress."><div className="grid gap-6 lg:grid-cols-[1fr_.8fr]"> <div className="grid gap-4">{campaigns.map((c) => <Card key={c.title}><div className="flex flex-wrap justify-between gap-3"><h2 className="text-xl font-black">{c.title}</h2><Badge tone="violet">{c.status}</Badge></div><p className="mt-2 text-sm text-slate-600 dark:text-slate-300">{c.business} · {c.budget} · {c.deal}</p><div className="mt-4 flex flex-wrap gap-2">{c.deliverables.map((d) => <Badge key={d}>{d}</Badge>)}</div><progress className="mt-4 h-2 w-full" value={c.progress} max="100" aria-label="Campaign progress" /></Card>)}</div><Card><h2 className="text-xl font-black">Create offer draft</h2><form className="mt-4 grid gap-4" noValidate><Field label="Campaign title"><input className="rounded-2xl border p-3 dark:border-white/10 dark:bg-white/10" required /></Field><Field label="Budget or x-deal details"><input className="rounded-2xl border p-3 dark:border-white/10 dark:bg-white/10" placeholder="₱10,000 or product/stay details" required /></Field><Field label="Deliverables"><textarea className="min-h-28 rounded-2xl border p-3 dark:border-white/10 dark:bg-white/10" placeholder="1 Reel, 3 Stories, review notes" required /></Field><Button type="button"><ClipboardCheck className="h-4 w-4" /> Save draft</Button></form></Card></div></AppShell></ProtectedRoute>; }
