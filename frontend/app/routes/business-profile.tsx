import { Building2, Mail, MapPin } from "lucide-react";
import { Link, useParams } from "react-router";
import { Badge, Button, Card, StatusPanel, VerifiedMark } from "~/components/ui";
import { businesses } from "~/data/mock";

export function meta() { return [{ title: "Business profile | Collabify" }]; }
export default function BusinessProfile() { const { id } = useParams(); const b = businesses.find((item) => item.id === id) ?? businesses[0]; if (!b) return <StatusPanel type="error" title="Business not found" message="This profile is unavailable." />; return <main className="min-h-screen bg-slate-50 px-4 py-8 text-slate-950 dark:bg-slate-950 dark:text-white"><div className="mx-auto max-w-5xl"><Link to="/discover/businesses" className="text-sm font-bold text-cyan-700">← Back to businesses</Link><Card className="mt-6"><Building2 className="h-10 w-10 text-cyan-500" /><div className="mt-4 flex flex-wrap items-center gap-3"><h1 className="text-4xl font-black">{b.name}</h1>{b.verified ? <VerifiedMark /> : <Badge tone="amber">Verification pending</Badge>}</div><p className="mt-2 text-slate-600 dark:text-slate-300"><MapPin className="inline h-4 w-4" /> {b.city} area · {b.industry}</p><p className="mt-5 max-w-2xl">{b.bio}</p><div className="mt-6 flex flex-wrap gap-2">{b.interests.map((i) => <Badge key={i} tone="cyan">{i}</Badge>)}</div><Button className="mt-8" disabled={!b.verified} type="button"><Mail className="h-4 w-4" /> Contact business</Button>{!b.verified && <p className="mt-2 text-sm text-amber-700">Contact options unlock after verification under Supabase policies.</p>}</Card></div></main>; }
