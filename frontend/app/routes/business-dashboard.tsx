import { CalendarCheck, Megaphone, Search, ShieldCheck } from "lucide-react";
import { AppShell, Badge, Card, ProtectedRoute, Stat } from "~/components/ui";
import { creators, campaigns } from "~/data/mock";

export function meta() { return [{ title: "Business dashboard | Collabify" }]; }
export default function BusinessDashboard() { return <ProtectedRoute><AppShell role="business" title="Business dashboard" description="Discover public creator cards, manage campaign offers, and monitor verification status."><div className="grid gap-5 md:grid-cols-4"><Stat icon={ShieldCheck} label="Verification" value="Pending" /><Stat icon={Search} label="Saved creators" value="18" /><Stat icon={Megaphone} label="Active campaigns" value="2" /><Stat icon={CalendarCheck} label="Meetings" value="3" /></div><div className="mt-6 grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><Card><h2 className="text-xl font-black">Recommended creators</h2><div className="mt-4 grid gap-3">{creators.map((c) => <article key={c.id} className="rounded-2xl bg-slate-50 p-4 dark:bg-white/10"><div className="flex items-start justify-between gap-4"><div><h3 className="font-bold">{c.name}</h3><p className="text-sm text-slate-600 dark:text-slate-300">{c.city} · {c.followers} followers · {c.engagement}</p></div><Badge tone="green">Safe public fields</Badge></div></article>)}</div></Card><Card><h2 className="text-xl font-black">Campaign health</h2>{campaigns.map((c) => <div key={c.title} className="mt-4 rounded-2xl bg-slate-50 p-4 dark:bg-white/10"><Badge tone="violet">{c.status}</Badge><h3 className="mt-2 font-bold">{c.title}</h3><progress className="mt-3 h-2 w-full" value={c.progress} max="100" /></div>)}</Card></div></AppShell></ProtectedRoute>; }
